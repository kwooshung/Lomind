import values from './values';
import mixedTypes from './mixedTypes';

export { default as values } from './values';
export { default as mixedTypes } from './mixedTypes';

export default {
  values,
  mixedTypes
};
